/*
package logica;

import java.util.Scanner;

public class ReverseLines {
    String reverse;
    public ReverseLines(String reverse){
        this.reverse = reverse;
    }
    public String ReverseLines(String text){
        int lengte = text.length();
        for (int i = 0; i<=text.length()-1; i++){
            reverse = reverse + text.substring(lengte-1, lengte);
            lengte--;
        }
        return reverse;
    }
}
*/