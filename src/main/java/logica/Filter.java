/*
package org.example;

import java.util.Scanner;

public class Filter {
    public static void main(String[] args) {

        String woord = "Bloemkool";
        int aantal = 9; /* hierdoor pakt het de woorden alleen langer dan 8  */   /*

        String [] groenten = {"Wortel", "Sperzieboontjes", "Broccoli", "Bloemkool", "Spinazie", "Sla"};
        String [] groentenAcht = {"Sperzieboontjes", "Bloemkool"};

        String [] correct = filter(groenten, aantal);
        filter(woord, aantal);

        for (String print: correct
        ) {
            System.out.println(print);
        }
    }

    public static boolean filter(String woord, int aantal){

        if (woord.length() >= aantal){ /* Dus als 8 kleiner is of gelijk aan 9 dan is het woord correcte lengte*/  /*
            return true;
        } else {
            return false;
        }

    }public static String [] filter(String[]groenten, int aantal){


        String [] correct = new String[2]; /* Er zijn maar 2 woorden langer dan 8 */  /*
        int count = 0;

        for (int i = 0; i <= groenten.length-1 ;i++){
            if (filter(groenten[i],aantal)){ /* Als de methode true is voer dan de methode uit */  /*
                correct[count] = groenten[i];
                count++;
            }
        }

        return correct;
    }
}

*/
